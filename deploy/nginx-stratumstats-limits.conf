# This file is included from nginx's http context by setup-nginx.sh.
limit_req_zone $binary_remote_addr zone=stratumstats_ingest:10m rate=10r/m;
limit_conn_zone $binary_remote_addr zone=stratumstats_per_ip:10m;
